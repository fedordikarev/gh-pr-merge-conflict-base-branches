name: Conclusion From Another Branch

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - id: welcome-message
	name: Echo Welcome Message from Another Branch
        run: |
          echo "I know your name ${{ github.actor }}"
          echo "And I don't miss base branch creating this PR"
          echo "❤️ Welcome to the branch! Have a great day! 😊"
